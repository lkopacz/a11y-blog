{% extends '@classy/block/block.html.twig' %}
{% block content %}
    <div class="block--bundle-hero__wrapper">{{ parent() }}</div>
    {% if is_front %}
        <div class="block--bundle-hero__down-arrow">
            <i aria-hidden="true" class="fa fa-angle-down"></i>
        </div>
    {% endif %}
{% endblock %}
